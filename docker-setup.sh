# `python` is linked to 2.7 instead of 3, change that:
sudo ln -sf /usr/bin/python3 /usr/bin/python

# install docker
sudo apt-get update
sudo apt-get -y install docker docker.io docker-compose

# install python stuff
# sudo apt-get -y install python3-pip
# sudo pip3 install jupyter

# No, look, this looks more complicated than the docker solution:
# https://medium.com/@alexjsanchez/python-3-notebooks-on-aws-ec2-in-15-mostly-easy-steps-2ec5e662c6c6
